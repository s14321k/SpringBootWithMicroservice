# Kafka

- Search `Apache kafka download`
- Select `GetStarterd` -> `Quick Start`
- Under `Step 1` select the `download` link.
- Select the first download link which has `.tgz`

## [Commands](https://kafka.apache.org/quickstart)

- Before
